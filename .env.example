# Backend URL — no trailing slash
# Development: leave blank, Vite proxy handles /api/* automatically
# Production:  set this in your Vercel dashboard → Project → Settings → Environment Variables
VITE_API_URL=https://your-backend.onrender.com
